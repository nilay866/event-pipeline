# event-pipeline
## Lambda Deployment

Lambda source code is available in the /lambda directory.
The deployment package (lambda.zip) is generated during deployment.
Terraform files represent the infrastructure definition.
